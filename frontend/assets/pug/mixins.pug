mixin icon(className, title)
  if title
    i.fa(class=className aria-hidden='true' data-toggle='tooltip' data-placement='top' title=title)
  else
    i.fa(class=className aria-hidden='true')

mixin option
  - var n = 0
  while n < 5
    - var t = 'Lorem - ' + n++
    option= t